version: '3.8'

services:
  nginx:
    volumes:
      - ./nginx/certs/selfsigned.crt:/etc/nginx/certs/fullchain.pem:ro
      - ./nginx/certs/selfsigned.key:/etc/nginx/certs/privkey.pem:ro
  postgres:
    ports:
      - '5432:5432'
  nextjs:
    ports:
      - '3000:3000'
